package unit09;
import java.util.Comparator;
public class FruitComparator implements Comparator<Fruit>
{
    public int comapre(Fruit f1, Fruit f2)
    {
        double diff = f1.getPrice() - f2.getPrice()
        if(diff == 0)
        {
            
        }
    }
}
